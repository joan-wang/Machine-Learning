import pandas as pd
import numpy as np
from config import *
from sklearn.model_selection import train_test_split


def split(df, features, y):
    pass
    #return X_train, X_test, y_train, y_test = train_test_split(df[features], y, test_size=TEST_SIZE, random_state=17)

